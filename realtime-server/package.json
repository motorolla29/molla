{
  "name": "molla-socket-server",
  "version": "1.0.0",
  "description": "Socket.IO server for Molla real-time chat",
  "main": "server.js",
  "scripts": {
    "build": "echo 'Prisma client will be generated in Railway build command'",
    "start": "node server.js",
    "postinstall": "echo 'Postinstall skipped - using Railway build command instead'"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cookie": "^0.7.1",
    "jsonwebtoken": "^9.0.3",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}